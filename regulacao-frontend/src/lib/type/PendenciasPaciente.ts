export type PacientePendentes = {
    id: number | null,
    cns: string,
    cpfPaciente: string,
    especialidades: string,
    dataNascimento: [number, number, number],
    nomePaciente: String,
}

